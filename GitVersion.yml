assembly-versioning-scheme: MajorMinorPatch
mode: ContinuousDelivery
next-version: 1.5.0
branches:
  main:
    regex: ^master$
  master:
    regex: ^master$
  pull-request:
    tag: pr
ignore:
  sha: []
merge-message-formats: {}
